{
    "ci": {
      "server": {
        "storage": {
          "sqlDialect": "postgres",
          "sqlConnectionUrl": "postgres://postgres:${POSTGRES_PASSWORD}@${TEST_INFRA_POSTGRES_HOST:-postgres}/${PROJECT_NAME}_lighthouse?application_name=lighthouse-ci-server"
        }
      }
    }
}
